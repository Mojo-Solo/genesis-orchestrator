# Zen MCP Server Environment Configuration
# Generated by LOCKSMITH-AUTH-ENFORCER
# Timestamp: 2025-08-21T16:15:33+00:00
# Organization: Mojo-Solo
#
# SECURITY NOTICE: This file contains API keys from GitHub organization secrets
# File permissions: 600 (owner read/write only)

# ============================================================================
# API KEYS - CONFIGURED FROM MOJO-SOLO ORGANIZATION
# ============================================================================

# These keys are managed through GitHub organization secrets and should not be
# modified manually. Use the sync script to update from the organization.

# OpenAI API Key - Available in organization secrets
OPENAI_API_KEY=${GITHUB_ACTIONS_OPENAI_API_KEY:-placeholder_openai_api_key}

# Gemini API Key - Available in organization secrets  
GEMINI_API_KEY=${GITHUB_ACTIONS_GEMINI_API_KEY:-placeholder_gemini_api_key}

# Anthropic API Key - Available in organization secrets
ANTHROPIC_API_KEY=${GITHUB_ACTIONS_ANTHROPIC_API_KEY:-placeholder_anthropic_api_key}

# OpenRouter API Key - Available in organization secrets
OPENROUTER_API_KEY=${GITHUB_ACTIONS_OPENROUTER_API_KEY:-placeholder_openrouter_api_key}

# ============================================================================
# ZEN MCP CONFIGURATION - OPTIMIZED FOR MULTI-MODEL ORCHESTRATION
# ============================================================================

# ULTRATHINK MODE - GPT-5 WITH REASONING TOKENS (August 2025)
# GPT-5 as primary model with maximum reasoning capability
DEFAULT_MODEL=gpt-5

# ULTRATHINK: Maximum reasoning effort for GPT-5 reasoning tokens
DEFAULT_THINKING_MODE_THINKDEEP=high

# GPT-5 reasoning configuration
GPT5_REASONING_EFFORT=high
GPT5_VERBOSITY=high
GPT5_MAX_REASONING_TOKENS=128000

# Model restrictions - GPT-5 PRIORITIZED (400K context, reasoning tokens)
OPENAI_ALLOWED_MODELS=gpt-5,gpt-5-mini,gpt-5-nano,o4-mini,mini,o3-mini
GOOGLE_ALLOWED_MODELS=flash,pro
# XAI_ALLOWED_MODELS=grok-3
# OPENROUTER_ALLOWED_MODELS=

# Conversation management
CONVERSATION_TIMEOUT_HOURS=3
MAX_CONVERSATION_TURNS=20

# Logging configuration
LOG_LEVEL=INFO

# Docker configuration
COMPOSE_PROJECT_NAME=zen-mcp
TZ=UTC
LOG_MAX_SIZE=10MB

# ============================================================================
# SECURITY CONFIGURATION
# ============================================================================

# This configuration is managed by the locksmith-auth-enforcer system
# Last synced: 2025-08-21T16:15:33+00:00
# Organization: Mojo-Solo
# Authentication chain: GitHub Org → Local Environment → MCP Server